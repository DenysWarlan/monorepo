name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm i

#      Auth login tests
      - run: npm run auth-login:test:ci
      - run: npm run auth-login:e2e:production

#      Auth register tests
      - run: npm run auth-register:test:ci
      - run: npm run auth-register:e2e:production

#      Books search tests
      - run: npm run books-search:test:ci
      - run: npm run books-search:e2e:production

#      Books list tests
      - run: npm run books-list:test:ci

#     Build apps
      - run: npm run build:api:production
      - run: npm run build:web:production
#
#  deploy-web:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MONOREPO_D81DF }}'
#          channelId: live
#          projectId: monorepo-d81df
#
#  deploy-api:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#
#      - name: Use Node 18
#        uses: actions/setup-node@v1
#        with:
#          node-version: 18.x
#
#      - name: Install Railway
#        run: npm i -g @railway/cli
#
#      - name: Deploy
#        run: railway up
#        env:
#          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}